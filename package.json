
{
  "author": "Ian Serlin",
  "name": "now-sessions",
  "description": "Uses now-middleware for NowJS to enable HTTP session lookup from a Connect-compliant session store when a new nowjs session is created.",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git://github.com/ianserlin/now-sessions.git"
  },
  "main": "lib/sessions.js",
  "engines": {
    "node": ">= v0.4.0"
  },
  "dependencies": {
    "now": ">= 0.7.6",
    "now-middleware": ">= 0.0.1"
},
  "devDependencies": {}
}
